let form = document.querySelector('form');let input_task = document.getElementById('task');let task_list = document.querySelector('.list-group');let clear_btn = document.querySelector('.clear-tasks ');let filter = document.getElementById('filter');loadEvents();function loadEvents(){    form.addEventListener('submit' , addTask)    clear_btn.addEventListener('click' , delete_all)    task_list.addEventListener('click' , delete_task)    filter.addEventListener('keyup' , filter_tasks)}function addTask(e){    let li = document.createElement('li');    li.className = 'list-group-item d-flex align-items-center'    let i = document.createElement('i');    i.className = 'fas fa-times text-danger mr-auto delete-item'    let task_text = document.createTextNode(input_task.value)    li.appendChild(task_text)    li.appendChild(i)    task_list.appendChild(li)    input_task.value = ''    e.preventDefault();}function delete_task(e){    if (e.target.classList.contains('delete-item'))    {        e.target.parentElement.remove();    }}function delete_all(e){    task_list.innerHTML = ''    e.preventDefault()}function filter_tasks(e){    let filter_letters = e.target.value.toLowerCase();    let tasks = document.querySelectorAll('.list-group-item')    tasks.forEach(function (index)    {        if (index.textContent.toLowerCase().indexOf(filter_letters) === -1)        {            index.style.display = 'none'        }else        {            index.style.color = 'block'        }    })}